# Journal IA — Mini-TP 3 — Fanou

- Ligne : bonne, c'est la seule ligne du code qui est à nous.
- Cause : à moitié bonne. Elle reste vague avec 2 hypothèses, mais en vrai le XML est correct (`btnPartager` existe bien), donc c'est le Kotlin qui a une faute de frappe, pas le layout qui a changé.
- Correction : dans le bon sens (comparer les ids) mais trop vague, fallait juste dire : remplacer `btnPartage` par `btnPartager` dans le code.
